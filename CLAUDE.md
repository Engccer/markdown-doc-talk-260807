# 슬라이드덱: 260807 한국시각장애교육재활학회 학술대회 발제

이 파일은 Claude Code(claude.ai/code)가 이 저장소에서 작업할 때 참고하는 가이드다.

## 행사 컨텍스트

- **행사명**: 제45차 한국시각장애교육재활학회 학술대회
- **전체 주제**: 한글 점자 창안 100주년: 점자의 현재와 미래
- **일시**: 2026년 8월 7일(금) 10:00~17:30. 발제는 오전 분과발표 20분
- **배정 시간대**: 분과발표 2차(10:50~11:30)로 잠정. 주최 측 확인 전에는 확정으로 쓰지 않는다
- **장소**: 여의도 이룸센터
- **발제 제목**: AI와 마크다운을 활용한 HWP·DOCX 등 구조화된 텍스트 문서 작성 사례
- **발표자**: 김헌용 (신명중학교 영어 교사, 함께하는장애인교원노동조합 위원장)
- **연계 순서**: 발제 직후 김찬홍 알리접근연구소 대표가 발제에서 소개한 개념·워크플로우를 시연한다. 그래서 이 덱에는 도구 조작 시연을 넣지 않는다

## 이 덱의 성격: 발표용이지 실습 지시판이 아니다

직계 선행 덱인 `260806 대전 장애인교원...`은 참가자가 각자 노트북에서 따라 읽는 실습 지시판이었다. 이 덱은 다르다. 20분 학술 발표이고 청중은 듣는 자리에 있다.

- **지시문 복사 기능을 쓰지 않는다.** `.prompt` 블록과 복사 스크립트를 260806에서 승계하지 않았다. 실습이 없으므로 순수 비용이다
- **장식용 배경 이미지·생성 이미지는 만들지 않는다.** 청중에 스크린 리더 사용자가 많다. 정보가 없는 시각 요소는 이 청중에게 비용이다
- **저시력 참가자를 위해 큰 글씨·고대비를 기본값으로.**
- **읽히는 순서 = 말하는 순서.** 시각적 배치로만 성립하는 2열·2x2 그리드는 쓰지 않는다

접근성 규칙의 정본은 글로벌 접근성 헌장(`~/.claude/ACCESSIBILITY.md`)이다. 여기에는 이 저장소 고유 사항만 적는다. **과잉 ARIA 금지**가 그대로 적용된다. 유일한 landmark는 `<main>` 하나이고, 슬라이드 컨테이너는 `<section>`이 아니라 `<div class="slide">`다.

## 원고가 정본이다

슬라이드는 원고의 파생물이다. **원고에 없는 내용을 슬라이드에서 새로 만들지 않는다.** 내용을 바꿔야 하면 원고를 먼저 고친다.

**문구도 원고 것을 쓴다.** 제목은 원고의 소제목이나 원고 본문에 실제로 있는 문장에서 가져오고, 목록은 원고 문장의 어휘와 구문을 살려 개조식으로 줄이며, `.point`는 원고 문장을 그대로 옮긴다. 요약하다 보면 원고에 없는 표현이 만들어지는데(초판에서 "그 우회로가 정석이 되었습니다", "같은 조건, 같은 문서" 같은 제목이 그렇게 생겼다) 발표자가 입으로 말할 문장과 화면의 문장이 어긋나면 청중이 둘을 따로 처리해야 한다. 압축은 하되 다시 쓰지 않는다.

| 자산 | 경로 |
|---|---|
| 발제 원고 (SSoT) | `G:\내 드라이브\KHY\Lectures\260807 한국시각장애인재활학회 분임 토론\김헌용_발제원고.md` |
| 발제 개요 (주최 측 제출본) | 같은 폴더 `김헌용_발제개요.md` · `김헌용_발제개요.hwpx` |
| 문체 규칙 | 같은 폴더 `김헌용 문체 규칙.md` |
| 원천 글 모음 | 같은 폴더 `내 글 모음\` |

상위 강연 폴더 이름은 `260807 한국시각장애인재활학회 분임 토론`이지만 실제 학회명은 **한국시각장애교육재활학회**이고 형태는 **분과 발표**다. 이 덱 폴더와 슬라이드 표기는 정확한 쪽을 쓴다.

## 슬라이드 구성

23장. 원고의 4부 구조를 그대로 따르되 슬라이드 단위로 재분절했다.

| 범위 | 내용 |
|---|---|
| 1~4 | 표지, 도입 장면(시험 원안), 표제 논지, 목차 |
| 5~9 | 1부. txt가 담지 못하는 것, 스크린 리더 사용자에게 달라지는 것, 크롤러와의 동형성, 작업 순서 세 단계 |
| 10~13 | 2부. 한글 문서의 벽, 스킬이란, 함정 셋, 스킬이 하는 세 가지 일과 대조 |
| 14~17 | 3부. 사례 넷, 세 번째 대조, 관통하는 흐름과 검증의 층위, 책임 공백 |
| 18~23 | 4부. 마지막 구간, 권리 옹호, 약점이 강점이 되는 조건, 남은 과제, 나가며, 공개 도구 |

20분에 23장이면 장당 50초 남짓이다. **슬라이드는 원고의 요약이 아니다.** 할 말은 원고에 있고 현장에서 구두로 푼다. 슬라이드는 그 말과 말 사이를 잇는 장치이므로 장수를 줄이려고 한 장에 내용을 몰지 않는다.

### 각 장의 결론 한 줄: `.point`

슬라이드마다 마지막에 `<p class="point">`로 그 장의 결론을 한 문장 둔다(목차인 4장과 링크 목록인 23장은 결론이 성립하지 않으므로 예외다). 목록은 명사형·개조식이고 이 한 줄만 존댓말 서술형이다. 발표자가 그 장에서 반드시 말해야 하는 문장이 무엇인지 표시하는 자리이자, 청중이 한 장에서 가져갈 것을 남기는 자리다. 장식이 아니므로 내용을 고칠 때 이 줄을 먼저 확정하고 목록을 맞춘다.

### 표를 쓰지 않는다

원고에 수치 데이터가 없고, 스크린 리더로 표를 훑는 비용이 목록보다 크다. 마크다운 기호 설명(5장)도 `<table>` 대신 `.mark` 블록 안의 짧은 문단 셋으로 뒀다.

같은 이유로 5장에 **실제 마크다운 기호(`# 제목`, `- 목록`)를 그대로 보여 주지 않는다.** 시각 청중에게는 실물 예시가 이해를 앞당기지만, 스크린 리더는 구두점 설정에 따라 `#`를 아예 읽지 않아 그 줄이 의미를 잃는다. 청중의 다수가 후자이므로 "우물 정 자를 앞에 붙이면 제목"처럼 기호를 말로 적는 쪽을 택했다. 시각 청중용 예시를 넣고 싶으면 기호와 이름을 한 줄에 합치되, 그 줄이 기호 없이도 읽히는지 먼저 확인한다.

### 링크에 주소를 병기한다

23장의 링크는 이름 뒤에 주소를 쉼표로 이어 한 줄로 뒀다(`시각장애인을 위한 스킬 모음, github.com/Engccer/skills-for-the-blind`). 현장에서 주소를 받아 적으려면 화면에 보여야 하고, 이름과 주소를 별도 요소로 나누면 스크린 리더에서 객체가 둘로 쪼개진다. 프로토콜(`https://`)은 낭독 노이즈라 표시 문자열에서 뺀다.

## 기술 스택

260806 덱의 엔진을 그대로 승계했다. 단일 `index.html`(바닐라 HTML/CSS/JS), 외부 의존 없음, CDN 폰트도 쓰지 않는다(행사장 네트워크 실패 대비).

- 키보드 내비게이션: 화살표 / Home / End / PageUp / PageDown / Backspace
- 슬라이드 전환 시 해당 슬라이드의 제목으로 포커스 이동(스크린 리더가 새 슬라이드를 읽도록)
- 상단 토글 셋: 문서 모드, 내레이션, 효과음. 선택은 localStorage에 남는다

### 문서 모드

발표 모드(한 장씩)와 문서 모드(전체를 한 페이지로)를 같은 DOM에 CSS로만 구현한다. 청중이 발표자 진도와 무관하게 제목 이동으로 자기 속도로 읽는 경로다. 내용을 두 벌로 만들지 않는다. 문서 모드에서는 내레이션을 멈춘다.

### 내레이션

슬라이드마다 `narration/slide-NN.mp3` 하나씩 23개. 총 71초, 평균 3.1초로 전환 신호에 가깝다.

- **내용은 제목 + 한 줄 키워드까지만.** 본문은 스크린 리더가 읽으므로 같은 내용을 다시 읽으면 중복이다
- **제목을 고치면 그 트랙을 반드시 다시 뽑는다.** 해당 mp3만 지우고 스크립트를 인자 없이 실행하면 없는 것만 생성된다. 화면의 제목과 귀로 듣는 문장이 어긋나는 것이 가장 나쁘다
- **문안이 너무 짧으면 음량이 떨어진다.** "나가며" 세 글자로 뽑았더니 0.56초에 최대 -9.7 dB로 다른 트랙보다 9 dB 낮게 나왔다. 제목이 짧은 장은 키워드 한 줄을 붙여 2초 이상으로 만든다
- **1.1초 지연 후 시작한다**(`NARR_DELAY`). 스크린 리더가 슬라이드 제목을 먼저 읽도록 비켜 주는 간격이다. 0으로 만들면 제목 낭독과 정면으로 겹친다
- **연달아 넘기면 앞 트랙을 멈춘다**(`stopNarration`)
- **파일이 없거나 브라우저가 자동재생을 막으면 조용히 넘어간다.** 내레이션 실패가 진행을 막지 않는다

```powershell
cd "C:\Users\pc\Windows-Projects\education\blindusingai\260807 한국시각장애교육재활학회 학술대회 발제"
python _generate_narration.py            # 없는 것만
python _generate_narration.py --force    # 전부 다시
```

ElevenLabs `eleven_v3`, 음성은 Hyunsu(`cuXUjH0CSJkKipo0Hy9i`, 한국어 남성 팟캐스트 진행 톤). 센스리더 기본 음성과 구별되도록 남성 음성을 골랐다. `ELEVENLABS_API_KEY` 환경변수가 필요하다.

**약어·영문 표기·숫자는 한글 발음으로 적는다.** TTS가 철자대로 읽어 버린다. 스크립트에서는 "티엑스티", "에이아이", "제 사십오차"로 적었다. 문안을 고칠 때 이 규칙을 빠뜨리지 않는다.

**음량 확인 (2026-08-07 실측).** 최대 -0.7~-2.1 dB, 평균 -14.7~-18.5 dB로 스크린 리더 음성 위로 충분히 들린다. 재생 볼륨은 1로 두고 코드에서 낮추지 않는다. 파일을 갈아 끼울 때는 `ffmpeg -i <파일> -af volumedetect -f null -`로 레벨부터 확인한다.

### 효과음

`sfx/page-turn.mp3`를 시작할 때 미리 로드해 두고, `readyState >= 2`일 때만 그 파일을 쓴다. 준비가 안 됐으면 Web Audio 합성음으로 넘어간다.

**소리 크기 함정(260806에서 이월).** 260523 덱의 원본 `page-turn.mp3`는 최대 -25.4 dB로 극단적으로 작아 스크린 리더 음성 아래에서 들리지 않았다. `ffmpeg -af "volume=22dB"`로 최대 -3.9 dB까지 보정한 파일이 여기 들어 있다. 재생 볼륨을 임의로 낮추지 않는다.

정지된 `AudioContext`에 오실레이터를 예약하면 소리가 사라지므로, 합성음은 `resume()`이 끝난 뒤에 예약한다.

## 톤·표기 규칙

- **em dash(—)·en dash(–) 전면 금지.** 가운뎃점(·) 또는 콜론으로 대체한다
- **"닿다/가닿다" 금지** (이 저장소 공통 규칙)
- **UI 컨트롤 라벨에 이모지 금지**
- 청중이 연구자·현장 교사이므로 시혜적 표현("도움을 받아", "극복") 대신 실무자 어조를 유지한다
- **모든 외부 링크는 새 탭으로 연다.** `target="_blank" rel="noopener"`
- **분과발표 2차를 확정 사실로 쓰지 않는다.** 주최 측 확인 전까지 잠정

## 문체

**목록은 명사형·개조식, `.point`는 존댓말 서술형**이라는 대비를 유지한다. 목록 항목을 문장으로 늘어뜨리지 않고, `.point`를 개조식으로 깎지 않는다.

원고가 존댓말 서술형(`~습니다`)이므로 `.point`도 거기 맞춘다. 발제 개요만 음슴체이고 이 덱은 아니다.

## 서술 층위 (가장 중요)

청중은 시각장애 교육·재활 연구자와 현장 교사이며, 뒤이어 도구 시연 순서가 따로 있다. 발제는 기술의 장면이 아니라 **경험의 장면**으로 서술한다.

- 쓰지 않는다: 파일 내부 구조, 좌표값, 스키마 검증 결과, 스크립트·라이브러리 이름, 수치화된 정확도
- 쓴다: 예전에는 어디서 막혔고 지금은 무엇이 달라졌는가, 그것이 당사자에게 무슨 의미인가
- 판별 질문: 이 문장이 도구의 작동을 설명하는가, 사람의 경험을 설명하는가. 전자면 한 층 위로 올린다

## 로컬 확인

```powershell
cd "C:\Users\pc\Windows-Projects\education\blindusingai\260807 한국시각장애교육재활학회 학술대회 발제"
python -m http.server 8765
# http://localhost:8765/
```

변경 후 반드시 확인할 것: 키보드로 처음부터 끝까지 이동되는지, 슬라이드마다 제목이 낭독되는지, 내레이션이 제목 낭독 뒤에 들어오는지.

## 배포

- **GitHub Pages 배포.** 저장소 `Engccer/markdown-doc-talk-260807`(공개), 배포 URL은 **https://engccer.github.io/markdown-doc-talk-260807/** 이다. `main` 브랜치 루트를 그대로 서빙하므로 push하면 1~2분 뒤 반영된다.
- 이 덱은 `education/blindusingai/` 안에 있지만 **자체 git 저장소**다. 상위 폴더는 git 관리 밖이므로 커밋·push는 이 폴더에서 한다.

**`file://`에서도 깨지지 않게 유지한다.** `fetch`를 쓰지 않고 상대 경로 오디오만 쓰는 현재 구조가 그 조건이다. 행사장 네트워크가 막히면 폴더째 옮기는 경로로 폴백한다.

## 관련 자산

- **상위 강연 폴더**: `G:\내 드라이브\KHY\Lectures\260807 한국시각장애인재활학회 분임 토론\` (원고·개요·문체 규칙·원천 글)
- **직계 선행 덱**: `..\260806 대전 장애인교원 맞춤형 AI·에듀테크 역량 강화 연수\` (엔진·내레이션 스크립트의 출처)
- **같은 학회 앞선 발표**: `..\260403 2026 한국시각장애교육재활학회 춘계 세미나\`
- **미착수 산출물**: 블로그 글(상위 강연 폴더 CLAUDE.md의 진행 상태 표 참조)
